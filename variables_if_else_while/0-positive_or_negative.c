#include <stdlib.h>
#include <time.h>
/**
 * main - Positive anything is better than negative nothing
 *
 * Return: 0
 */

int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	/* random int between 0 and 2 */
	return (0);
}
