#include "main.h"
#include <stdio.h>
/**
 * *_strcat - string to be added to
 * @dest: string to be added to
 * @src: stringt to be added
 * Return: value of the cat strings
 */
char *_strcat(char *dest, char *src)
{
	char *dest, *src;

	while (src 
