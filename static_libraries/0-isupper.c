#include "main.h"
/**
 * _isupper - checking for uppercase
 * @c: character to be checked
 * Return: return a 1 for an uppercase letter and a 0 for anything else
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')

	return (1);

	return (0);

}
