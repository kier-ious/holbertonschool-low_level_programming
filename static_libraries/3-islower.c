#include "main.h"
/**
 * _islower - checking for lowercase
 * @c: character to be checked
 * Return: return either a 0 or a 1
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')

	return (1);

	return (0);

}
