#include <stdio.h>
#include "main.h"
/**
 * is_prime_number - write function if prime
 * @n: number
 * Return: return number
 */
int is_prime_number(int n)
{
	if (n == 0)
		return (0);
	else
		return (1);
}
